import React from "react";
import { Container } from "react-bootstrap";

const Users =() =>{
    return(
        <Container className ="mt-4">
            <h2>Manage Users</h2>
            <p>Use this to add,remove or update user accounts.</p>
        </Container>
    );
};

export default Users;